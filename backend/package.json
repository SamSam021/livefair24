{
  "name": "livefair24-backend",
  "version": "1.0.0",
  "description": "LiveFair24 live-events platform backend. Plug-in provider architecture for ticket, hotel, and email APIs, plus a PostgreSQL-backed generic LiveEvent data model. DynamoDB (optional) still handles simple config/watcher blobs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:migrate": "node db/migrate.js",
    "db:seed-events": "node db/seed-live-events.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "pg": "^8.11.0"
  }
}
