version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - cd backend && npm install --omit=dev
run:
  runtime-version: 18
  command: node backend/server.js
  network:
    port: 8080
    env: PORT
  env:
    - name: PORT
      value: "8080"
